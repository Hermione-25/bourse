// ✅ Le fichier auth.routes.ts a été marqué comme fichier mort (plus de module à exporter).
// Cet index.ts est conservé vide pour éviter les erreurs de build.
// Supprimer les deux fichiers (auth.routes.ts et index.ts) lorsqu'ils ne sont plus référencés.

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: mon-frontend:latest
    container_name: frontend
    restart: unless-stopped
    ports:
      - "8080:80"   # adaptez le port externe selon vos besoinsservices:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: mon-frontend:latest
    container_name: frontend
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
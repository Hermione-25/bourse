services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: mon-frontend:latest
    container_name: frontend
    restart: unless-stopped
    ports:
      - "8080:80"   # adaptez le port externe selon vos besoins
    networks:
      - webnet

networks:
  webnet:
    driver: bridge